import Empty from "../../components/issues/empty";
/**
 * @returns: component containing a lottie animation and message to
 * select one of the issues
 */
export default function IssuesPage() {
  return (
    <div className="h-full w-full border-2 bg-zinc-200 rounded-xl">
      <Empty />
    </div>
  );
}
